[mysqld]
server-id = 223344
log_bin = mysql-bin
binlog_format = ROW
binlog_row_image = FULL
expire_logs_days = 10
# GTIDs are available in MySQL 5.6.5 and later
gtid_mode = ON
enforce_gtid_consistency = ON
